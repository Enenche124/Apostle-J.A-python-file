import operator
def get_result(first_value, second_value):
   if type(first_value) == str:
        return 'invalid input'
   return operator.mul(first_value, second_value)
   return operator.mul.round((first_value, second_value), 3)
    